
'use client';
// This file is kept for structure, but its content is now in auth-context.tsx
// to avoid circular dependencies and for better organization when using the context.
// You can directly import useAuth from '@/contexts/auth-context';

export { useAuth } from '@/contexts/auth-context';
